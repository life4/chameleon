## Usage

First of all, copy and edit config:

```bash
cp config{_example,}.toml
nano config.toml
```

Run from source:

```bash
go run *.go
```

Build:

```bash
go build .
```

Run from build:

```bash
./chameleon
```